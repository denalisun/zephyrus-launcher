[package]
name = "zephyrus-launcher"
version = "0.1.0"
edition = "2021"

[dependencies]
reqwest = { version = "0.12.9", "features" = ["blocking"] }
tokio = { version = "1.41.1", features = ["full"] }
winapi = { version = "0.3", features = ["processthreadsapi", "handleapi"] }
